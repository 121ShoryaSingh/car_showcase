"use client"

import React from 'react'
import Image from 'next/image'

const CustomButton = () => {
  return (
    <button 
        disabled={false}
        type={"button"}
        className={'custom-btn'}
        onClick={() => {}}
    >
        sdsd
    </button>
  )
}

export default CustomButton